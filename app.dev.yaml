service: api-dev
runtime: nodejs22

instance_class: B1  # Customize as needed, e.g., F2 for more resources

manual_scaling:
  instances: 1

env_variables:
  MONGO_URI: mongodb://34.121.50.235:27017/
  DB_NAME: test_users_dev
  DB_USER: admin
  DB_PASS: admin@123
  API_PORT: 8080
  API_ENDPOINT: https://api-dev.osposstore.online/api
  NODE_ENV: dev

handlers:
- url: /.*
  script: auto
